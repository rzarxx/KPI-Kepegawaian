import InputError from '@/Components/InputError';
import { Button } from '@/Components/ui/button';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head, Link, useForm } from '@inertiajs/react';
import { ReactNode } from 'react';

type Option = { id: number; name: string; branch_id?: number; division_id?: number };
export default function Create({ branches, divisions, subDivisions, positions }: { branches: Option[]; divisions: Option[]; subDivisions: Option[]; positions: Option[] }) {
    const { data, setData, post, processing, errors } = useForm({ employee_number: '', national_id: '', full_name: '', email: '', phone: '', birth_date: '', gender: '', join_date: '', current_status: 'ACTIVE', branch_id: '', division_id: '', sub_division_id: '', position_id: '' });
    const filteredDivisions = divisions.filter((division) => !data.branch_id || division.branch_id === Number(data.branch_id));
    const filteredSubDivisions = subDivisions.filter((subDivision) => !data.division_id || subDivision.division_id === Number(data.division_id));
    const field = 'mt-1 h-10 w-full rounded-[9px] border-slate-300 text-sm focus:border-brand focus:ring-brand';
    return <AuthenticatedLayout header={<div><p className="text-sm text-slate-500">Karyawan</p><h1 className="text-[26px] font-bold text-slate-900">Tambah Karyawan</h1></div>}><Head title="Tambah Karyawan" /><div className="mx-auto max-w-4xl p-4 sm:p-6 lg:p-8"><form className="space-y-6" onSubmit={(event) => { event.preventDefault(); post(route('employees.store')); }}><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-base font-semibold text-slate-900">Identitas Karyawan</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Nomor Karyawan" error={errors.employee_number}><input className={field} value={data.employee_number} onChange={(e) => setData('employee_number', e.target.value)} /></Field><Field label="Nama Lengkap" error={errors.full_name}><input className={field} value={data.full_name} onChange={(e) => setData('full_name', e.target.value)} /></Field><Field label="NIK" error={errors.national_id}><input className={field} value={data.national_id} onChange={(e) => setData('national_id', e.target.value)} /></Field><Field label="Email" error={errors.email}><input className={field} type="email" value={data.email} onChange={(e) => setData('email', e.target.value)} /></Field><Field label="Nomor Telepon" error={errors.phone}><input className={field} value={data.phone} onChange={(e) => setData('phone', e.target.value)} /></Field><Field label="Tanggal Bergabung" error={errors.join_date}><input className={field} type="date" value={data.join_date} onChange={(e) => setData('join_date', e.target.value)} /></Field></div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-base font-semibold text-slate-900">Penempatan Awal</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Cabang" error={errors.branch_id}><select className={field} value={data.branch_id} onChange={(e) => { setData('branch_id', e.target.value); setData('division_id', ''); setData('sub_division_id', ''); }}><option value="">Pilih cabang</option>{branches.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Divisi" error={errors.division_id}><select className={field} value={data.division_id} onChange={(e) => { setData('division_id', e.target.value); setData('sub_division_id', ''); }}><option value="">Pilih divisi</option>{filteredDivisions.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Sub Divisi" error={errors.sub_division_id}><select className={field} value={data.sub_division_id} onChange={(e) => setData('sub_division_id', e.target.value)}><option value="">Tidak ada</option>{filteredSubDivisions.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Jabatan" error={errors.position_id}><select className={field} value={data.position_id} onChange={(e) => setData('position_id', e.target.value)}><option value="">Belum ditentukan</option>{positions.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Status Awal" error={errors.current_status}><select className={field} value={data.current_status} onChange={(e) => setData('current_status', e.target.value)}><option value="ACTIVE">Aktif</option><option value="PROBATION">Masa Percobaan</option><option value="INACTIVE">Tidak Aktif</option></select></Field></div></section><div className="flex justify-end gap-3"><Button asChild variant="secondary"><Link href={route('employees.index')}>Batal</Link></Button><Button disabled={processing} type="submit">{processing ? 'Menyimpan…' : 'Simpan Karyawan'}</Button></div></form></div></AuthenticatedLayout>;
}
function Field({ label, error, children }: { label: string; error?: string; children: ReactNode }) { return <label className="block text-sm font-semibold text-slate-700">{label}{children}<InputError className="mt-1" message={error}/></label>; }
